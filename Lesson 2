
'''
Name: George koniaris
Date: 9/26/2024
Description: More Strings

'''

my_int = 5
my_float = 6.38

print(f"{my_int} {my_float}")

another_float = 8.0 # makes this a float with 0
float_two = (float)(8) #makes a float by casting
print(f"{another_float} {float_two}")

#get decimal from a user
radius = float(input("Enter a radius"))
print(f"You entered a radius of {radius}")

#operations on numbers
# P E MModD AS
# Moduals remainder numeber
print(15 % 7) # prints the remainer when 15 is devided by 7
print((2+3)*4) # 2 + 3 first, times 4

# exponent is **

print(5**4) # this is 5^4


#weird math stuff
print(0.3-0.2) # roundo off error

num = 3.1415
print(round(num,2))
# method 2, use f string
print(f"{num:.2}")



#your turn to code
#ask a user for some amount of us dollars
# store this in a variable
# covert that into a currency of your choice
#

print("Enter an amount of US $ to convert into paper clips")
money = float(input("Enter a amount of US $ to convert"))


num = 3.1415
print(round(num,2))
